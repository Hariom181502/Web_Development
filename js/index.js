/* 1. Question one 
let userInput = prompt('Enter Number');

 if(userInput % 5 == 0){
     console.log(userInput,'is multiple of 5');
 }else{
    console.log(userInput,'is not multiple of 5');
 }

 let result = userInput % 5 == 0 ? `${userInput} is multiple of 5` : '${userInput} is not multiple of 5';

 console.log(result);
 */

//  -------------------------------------------------------------------
//  ------------------------------------------------------------------


 /* 2. Question two 

 
 let userMarks = prompt('Enter your marks and get grade');

if(userMarks >= 80 && userMarks <= 100)
{
    console.log(`Your marks is ${userMarks} and grade is 'A'`);
}
else if(userMarks >= 70 && userMarks <= 89)
{
console.log(`Your marks is ${userMarks} and grade is 'B'`);
}
else if(userMarks >= 60 && userMarks <= 69)
{
    console.log(`Your marks is ${userMarks} and grade is 'C'`);
}
else if(userMarks >= 50 && userMarks <= 59)
{
    console.log(`Your marks is ${userMarks} and grade is 'D'`);
}
else
{
    console.log(`Your marks is ${userMarks} and grade is 'F'`);
}

*/







// let createCard = (title,cHname,views,monthsOld,duration,thumbnail) => {
// };

// Cardbutton.addEventListener('click',createCard())